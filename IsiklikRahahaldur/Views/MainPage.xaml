<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:IsiklikRahahaldur.Models"
             x:Class="IsiklikRahahaldur.Views.MainPage"
             Title="Isiklik Rahahaldur">

    <Grid RowDefinitions="Auto, Auto, *, Auto" Padding="20" RowSpacing="15">

        <!-- Заголовок -->
        <Label Grid.Row="0"
               Text="Мой кошелек" 
               FontSize="32"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <!-- Отображение баланса -->
        <Border Grid.Row="1" StrokeShape="RoundRectangle 15" Padding="20" Stroke="LightGray">
            <VerticalStackLayout>
                <Label Text="Текущий баланс" FontSize="16" TextColor="Gray"/>
                <Label Text="{Binding Balance, StringFormat='{0:F2} €'}" 
                       FontSize="28" 
                       FontAttributes="Bold" 
                       HorizontalOptions="Center" />
            </VerticalStackLayout>
        </Border>

        <!-- Список транзакций -->
        <CollectionView Grid.Row="2" ItemsSource="{Binding Transactions}" Margin="0,10,0,0">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Transaction">
                    <Border Padding="10" Margin="0,5" StrokeShape="RoundRectangle 10" Stroke="Gainsboro">
                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding Description}" FontAttributes="Bold" />
                                <Label Text="{Binding Date, StringFormat='{0:dd.MM.yyyy}'}" FontSize="12" TextColor="Gray" />
                            </VerticalStackLayout>

                            <!-- Сумма с разным цветом для дохода и расхода -->
                            <Label Grid.Column="1" 
                                   VerticalOptions="Center" 
                                   FontAttributes="Bold"
                                   TextColor="{Binding IsIncome, Converter={StaticResource BoolToColorConverter}}">
                                <Label.Text>
                                    <MultiBinding StringFormat="{}{0}{1:F2} €">
                                        <Binding Path="IsIncome" Converter="{StaticResource BoolToSignConverter}" />
                                        <Binding Path="Amount" />
                                    </MultiBinding>
                                </Label.Text>
                            </Label>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Кнопки действий -->
        <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="10">
            <Button Grid.Column="0"
                    Text="+ Доход"
                    Command="{Binding GoToAddTransactionCommand}"
                    CommandParameter="True"
                    BackgroundColor="MediumSeaGreen"
                    TextColor="White"
                    CornerRadius="10"/>
            <Button Grid.Column="1"
                    Text="- Расход"
                    Command="{Binding GoToAddTransactionCommand}"
                    CommandParameter="False"
                    BackgroundColor="IndianRed"
                    TextColor="White"
                    CornerRadius="10"/>
        </Grid>
    </Grid>
</ContentPage>

